"use client";
import React from "react";
// import * as i from "@/index"; // CommonJS
// import { TextField, NewSubmitBtn, ActionDropdown } from "@/index"; // CommonJS

const TestPage = () => {
  // console.log("TestPage: ", i);
  return (
    <div className="container">
      {/* <TextField
        // names="school_name"
        names={`school_name`}
        types="text"
        labels="High School Name *"
        requireds={true}
      />
      <ActionDropdown
        items={[{ label: "Option 1", url: "localhost:3000", icon: "👍" }]}
      />
      <NewSubmitBtn labels="Submit" /> */}
    </div>
  );
};

export default TestPage;
